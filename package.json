{
  "name": "smart-attendance-system",
  "version": "1.0.0",
  "description": "Smart Attendance Management System using MERN stack",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "install-deps": "cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["attendance", "face-recognition", "qr-code", "mern"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}